import { Text, View } from "react-native";
import React from "react";
import Header from "../common/Header";
const Main = () => {
    return (
        <View style={{flex:1}}>
            <Header/>
        </View>
    )
}
export default Main;